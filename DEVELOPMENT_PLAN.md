# 终端安全检查与修复工具开发计划

状态：已确认并实施
计划版本：2.2
制定日期：2026-09-11（2026-09-16补充FTP扫描兼容修复计划）
作者：倚栏听雨

## 1. 开发目标

减少现有程序的版本和使用方式混乱，将终端处理流程统一为：

```text
修复前检查 → 保存原始结果 → 修复系统异常 → 修复后复检 → 生成对比报告
```

新版本只开发两种检查修复入口：

1. 现场检查修复版。
2. FTP 静默检查修复回传版。

HTTPS 回传版本次不开发、不修改。浏览器密码、USB 历史和无线网络历史继续由独立的违规记录清理程序处理。

现场版和 FTP 版必须使用同一套检查核心和同一套修复核心。两种版本的检查项目、判断标准、修复范围、执行顺序、复检标准和报告结论必须保持一致，不能分别实现两套逻辑。两者只能在是否显示界面、是否需要人工确认、是否自动打开报告以及是否通过 FTP 回传方面存在差异。

## 2. 程序划分

### 2.1 现场检查修复版

建议文件名：`onsite-check-repair.exe`

运行流程：

1. 执行修复前检查。
2. 显示发现的异常和需复核项目。
3. 现场人员输入小写 `yes` 后执行修复。
4. 未输入 `yes` 时不修改系统，只保存修复前检查报告。
5. 修复完成后自动执行复检。
6. 生成并打开修复前后对比报告。
7. 不自动重启系统。

### 2.2 FTP 静默检查修复回传版

建议文件名：`ftp-check-repair.exe`

运行流程：

1. 网管系统以已提权的管理员或 SYSTEM 身份推送并运行单个 EXE。
2. 后台静默执行修复前检查。
3. 自动修复允许处理的系统安全异常。
4. 自动执行修复后复检。
5. 生成 HTML 和 JSON 对比报告。
6. 将报告上传到现有 FTP 服务器。
7. 上传失败时，在程序同目录保留报告和 `upload-status.json`。
8. 不显示窗口、不要求输入、不自动重启。

### 2.3 违规记录清理版

建议文件名：`record-cleaner.exe`

该程序继续独立保留，不合并到自动修复流程。它负责：

- 清理当前用户浏览器已保存密码。
- 清理 USB 存储设备历史。
- 清理无线网络和手机热点历史。
- 输入小写 `yes` 后才执行。
- 生成临时清理报告。

独立保留的原因：

- 浏览器密码与当前登录用户及用户加密上下文有关，以 SYSTEM 身份运行可能清理不到实际用户的数据。
- USB、无线和浏览器记录属于不可逆的数据删除，应当先保存检查证据再清理。
- 删除无线配置可能影响正在使用无线连接的终端。
- 系统安全配置异常不应自动触发用户数据删除。

## 3. 通用 32 位终端程序方案

远程无法提前判断终端是 32 位还是 64 位，因此正式终端交付统一采用 Win7 兼容的 32 位程序。

正式推送文件：

```text
onsite-check-repair.exe
ftp-check-repair.exe
record-cleaner.exe
```

支持范围：

- Windows 7 SP1 32 位和 64 位。
- Windows 10 32 位和 64 位。
- Windows 11 64 位。

在 64 位系统中，32 位程序通过 WOW64 运行。实现时必须保证：

- 需要时明确访问 64 位注册表视图。
- 服务管理使用原生 Win32 服务接口。
- 防火墙管理使用 Windows 防火墙 COM 接口。
- SYSTEM 注册表、NetBIOS、RDP 和网络配置不受文件系统重定向影响。
- 浏览器检查覆盖普通用户配置及 32 位、64 位浏览器安装路径。
- 32 位程序在 64 位系统上的检查和修复结果与 64 位参考构建一致。

64 位构建继续保留在开发测试环境中，只用于一致性验证，不再作为网管推送入口，避免同一台 64 位终端被重复运行。

## 4. 自动修复范围

现场版和 FTP 版只自动修复系统安全配置。

所有修复规则集中在共享修复核心中维护。现场版和 FTP 版不得各自保存独立的端口列表、服务列表、注册表路径或合规判断条件。以后增加或修改规则时，只修改共享核心，并由两个入口同时使用。

### 4.1 防火墙

- 启用域、专用、公用三个 Windows 防火墙配置文件。
- 保证下列入站阻断规则各保留一条：

```text
TCP：22、135、136、139、445、3389
UDP：136、137、138、3389
```

- 删除本程序自身产生的同名重复规则。
- 不停止 RPC/RpcSs 核心服务。
- 不阻断普通网页访问和客户端访问外部文件服务器的出站能力。

### 4.2 文件共享

- 停止 `LanmanServer` 服务。
- 将 `LanmanServer` 启动类型设置为禁用。
- 不删除原有共享定义，以便需要时回滚。

### 4.3 远程桌面

- 停止 `TermService` 服务。
- 将 `TermService` 启动类型设置为禁用。
- 将 `fDenyTSConnections` 设置为 `1`，禁止远程桌面连接。
- 同时保留 TCP、UDP 3389 入站阻断规则。

### 4.4 NetBIOS

- 枚举全部网卡配置。
- 将每个网卡的 `NetbiosOptions` 设置为 `2（禁用）`。
- 修复后逐项复检，明确区分“没有记录”和“读取失败”。

### 4.5 不自动处理的内容

- 不自动删除浏览器密码。
- 不自动删除 USB 历史。
- 不自动删除无线网络和手机热点历史。
- 不自动禁用无线网卡。
- 不自动重启系统。
- 不绕过域策略、安全软件或终端管理策略。

### 4.6 已完成修复：终端FTP扫描接收兼容性

现场反馈：Windows FTP静默检查修复版运行后，部分终端原有的“FTP扫描”功能失效。当前代码不会停止或禁用`FTPSVC`、IIS FTP或第三方FTP服务，也没有直接阻断TCP 21；但程序会启用域、专用、公用三个防火墙配置文件。若终端原来依靠关闭防火墙接收扫描文件，或者缺少FTP控制端口及被动数据端口的入站允许规则，启用防火墙后可能造成业务中断。另有部分设备界面虽称“FTP扫描”，实际使用SMB共享目录；此类功能会与停止`LanmanServer`、禁用NetBIOS及阻断137、138、139、445的现行安全要求直接冲突。

实现必须先完成诊断，再决定修复策略，不得直接放开TCP 21或大范围被动端口：

1. 在修复前报告中增加“扫描接收兼容性”检查，记录`FTPSVC`/IIS FTP、可识别第三方FTP服务、实际监听端口、当前防火墙配置文件状态及现有入站允许规则；不读取或输出FTP账号密码。
2. 区分真正的FTP接收与SMB共享扫描。FTP由服务、监听端口和防火墙规则证据确认；UNC路径、共享目录、`LanmanServer`或445端口依赖归为SMB，不得继续笼统标记为FTP问题。
3. 不停止或禁用已识别的FTP服务，不删除非本程序创建的FTP允许规则。程序自身的10条高危端口阻断规则继续保持不变。
4. 如果已存在范围明确且有效的FTP控制端口和被动端口允许规则，启用防火墙后保留这些规则并复检监听和入站可达性。
5. 如果检测到FTP服务，但无法确定控制端口、被动端口范围、允许来源或现有规则是否完整，不自动创建“任意来源+大端口范围”规则；报告为“需复核”。已确认策略：保留原本关闭的防火墙配置文件，不自动启用它们，不开放未知端口；其他安全修复继续执行。管理员后续提供受控端口和来源网段并完善允许规则后，再启用相应配置文件。
6. 如果实际为SMB扫描，明确报告其与当前“关闭Server服务和445端口”的安全基线冲突。默认不恢复SMB；需要继续使用的终端应进入经授权的业务例外清单，或另行确认受限来源、受限共享和审计方案。
7. 批量部署前，先在一台承担扫描接收业务的终端使用`ftp-check-repair.exe /audit-only`核对证据，再用默认模式完成扫描实机验收；实机验收通过后才能扩大推送范围。
8. 现场版和FTP静默版继续共用同一诊断与修复核心，不能只在FTP回传入口增加特殊规则。

实现状态（2026-09-16）：已完成共享兼容性核心、Windows服务/TCP监听/防火墙允许规则/SMB共享取证、报告明细和已确认的防火墙保护策略；已完成纯策略、报告合规、共享逻辑及Win7静态导入自动测试。真实扫描终端的控制连接、认证、数据连接和文件落盘仍需实机验收。

本项完成标准：既不削弱10条高危端口阻断要求，也不把未知端口范围自动放开；真实FTP接收终端在启用防火墙后仍能完成控制连接、认证、数据连接和文件落盘；SMB扫描冲突能够被准确识别并交由授权策略处理。

## 5. 备份、幂等和回滚

- 第一次修复前保存防火墙、服务启动类型、服务运行状态、RDP 和 NetBIOS 原始状态。
- 已存在有效初始备份时不重复覆盖。
- 重复运行保持幂等，不重复创建防火墙规则。
- 修复后若配置被域策略或安全软件重新覆盖，应报告失败，不持续争抢策略。
- 保留 `/rollback`，只恢复本程序实际修改过的系统配置。
- 回滚不包含浏览器密码、USB 历史和无线历史等不可逆清理内容。

## 6. 报告格式和兼容性

继续使用现有报告文件名：

```text
verification-report_IP_MAC_时间.html
verification-report_IP_MAC_时间.json
```

报告内容分为三个阶段：

1. 修复前检查结果。
2. 修复动作及每项执行结果。
3. 修复后复检结果。

兼容要求：

- JSON 顶层 `summary` 和 `details` 保存修复后的最终结果。
- 新增修复前状态和修复过程字段，但不改变现有最终汇总字段的含义。
- HTML 明确对比每一项的修复前状态、修复动作和修复后状态。
- 报告不得包含 FTP 密码、浏览器密码、Cookie、令牌或其他敏感凭据。
- FTP 上传失败时保留完整本地报告和失败阶段、Win32 错误码。

## 7. 现有报告管理工具

以下程序继续使用，不重新开发：

- `ftp-report-pruner`：删除重复旧报告，只保留同一终端时间最新的一组 HTML、JSON。
- `report-issue-exporter`：按 IP 每台终端一行生成 Excel，并统计问题终端数量；人员对应表含可选的“组织机构”列时，将该值写入汇总表持有人之后。

为保持兼容，新版 FTP 报告仍使用原有文件名格式，JSON 顶层仍提供修复后的 `summary` 和 `details`。因此：

- 重复报告整理程序无需修改。
- Excel 汇总程序无需修改。
- Excel 默认统计修复后的最终结果。
- HTML 和 JSON 仍能保存完整的修复前后对比信息。

## 8. 交付目录规划

正式交付根目录使用仓库外部的本地 `delivery` 目录。所有新构建和更新先写入该目录；其他位置只允许在完成校验后人工复制，不作为构建源或优先交付目录。

```text
终端安全检查与修复工具
├─ 01-现场检查修复版
│  ├─ onsite-check-repair.exe
│  └─ README.txt
├─ 02-FTP检查修复回传版
│  ├─ ftp-check-repair.exe
│  └─ README.txt
├─ 03-违规记录清理版
│  ├─ record-cleaner.exe
│  └─ README.txt
└─ 04-报告管理工具
   ├─ ftp-report-pruner-x64.exe
   ├─ report-issue-exporter-x64.exe
   └─ README.txt
```

HTTPS 版本保留在源码和历史交付记录中，本次不修改，也不放入新的主要交付入口。

## 9. 版本管理建议

- 新终端套件当前发布版本为 `2.0.2`。
- 现场版和 FTP 版的产品版本为 `2.0.2.0`；记录清理版保持 `2.0.0.0`。
- 报告管理工具继续保留各自独立版本，并在交付清单中记录。
- 每次交付生成一份版本清单，记录文件名、文件版本、架构、SHA-256 和测试结论。
- 正式交付目录不包含源码、构建缓存、临时报告、测试日志、FTP 密码明文或签名私钥。
- 本地开发完成后不自动提交或上传 GitHub，必须等待明确指示。

## 10. 测试计划

### 10.1 公共行为接口

测试以下用户可观察行为：

1. 现场版能够完成修复前检查、确认、修复、复检和打开报告。
2. FTP 版能够静默完成检查、修复、复检、保存和上传报告。
3. 通用 32 位程序在 32 位和 64 位系统中得到一致的检查与修复结果。
4. 记录清理程序不会被检查修复流程自动调用。

### 10.2 自动测试

- 检查无异常时不执行多余修改。
- 已存在规则时不重复创建。
- 重复防火墙规则能够收敛为一条。
- Server、远程桌面和 NetBIOS 异常能够被修复并通过复检。
- 部分修复失败时，报告准确标出失败项目。
- 无管理员权限时拒绝修复并返回明确退出码。
- FTP 上传成功和上传失败两种路径均保留准确状态。
- 新报告能够继续被重复报告整理工具和 Excel 汇总工具识别。
- x86/x64 参考构建的 `summary`、`details` 和修复结果一致。
- 在相同的模拟系统状态下，现场版和 FTP 版的修复前 `summary`、`details` 必须逐项一致。
- 在相同的模拟系统状态下，现场版和 FTP 版生成的修复动作清单、执行结果和失败原因必须逐项一致。
- 修复完成后，现场版和 FTP 版的最终 `summary`、`details` 和退出结论必须逐项一致。
- 一致性测试忽略界面提示、报告是否自动打开和 FTP 上传状态，只比较检查与修复业务结果；任何业务差异都必须使测试失败。
- Win7 导入表不得包含 `CreateFile2` 等不兼容入口点。
- FTP接收服务不存在时，新增兼容检查不得改变现有修复动作和报告结论。
- FTP服务存在且已有精确允许规则时，修复不得停止服务、删除允许规则或阻断已声明的控制端口和被动端口。
- FTP服务存在但端口或规则不完整时，必须标记需复核，且不得自动创建任意来源的大范围允许规则。
- SMB扫描场景必须识别为`LanmanServer`/共享目录依赖，不能误报为TCP 21问题。
- FTP扫描兼容逻辑在现场版和FTP静默版中的检查、动作与复检结果必须一致。

### 10.3 真机验收

- Windows 7 SP1 x86。
- Windows 7 SP1 x64。
- Windows 10 x86。
- Windows 10 x64。
- Windows 11 x64。
- 普通管理员、已提权管理员和 SYSTEM 推送场景。
- 域策略覆盖、安全软件拦截、FTP 不通和重复运行场景。
- 真实FTP扫描接收：防火墙原为开启/关闭、主动/被动模式、控制端口21及自定义端口、限定被动端口范围、允许规则存在/缺失。
- 真实SMB扫描到共享目录：确认能够识别业务冲突，不以放开445或恢复`LanmanServer`冒充FTP兼容修复。

真机验收必须确认：

- 修复前报告准确。
- 修复动作与计划范围一致。
- 修复后报告准确。
- 从另一台电脑测试目标端口确实无法连接。
- 普通网页访问和客户端出站访问不受影响。
- 报告可以正常上传、整理并生成 Excel 汇总。
- Windows终端作为FTP接收端时，扫描设备能够完成连接、认证、数据传输和文件落盘；程序自身的FTP报告上传也保持正常。

## 11. 完成标准

- 正式终端只需要识别三个用途明确的通用 EXE。
- 网管远程只推送一次 FTP 通用版，不需要判断系统位数。
- 现场版和 FTP 版使用同一个检查核心和同一个修复核心。
- 两种版本的检查项目、合规判断、修复动作、失败处理和复检结论保持一致；版本差异仅限交互和回传方式。
- 修复前、修复过程和修复后结果可以在一份报告中准确对比。
- 现有重复报告整理和 Excel 汇总工具继续兼容。
- 所有自动测试通过。
- Win7 静态兼容检查通过。
- 目标系统真机测试结果单独记录，不用构建成功代替真机验收。

## 12. 开发边界

本计划确认前不修改现有程序。收到“确认可以开始”后，按以下顺序实施：

1. 固定检查、修复和报告的公共测试接口。
2. 提取共享系统修复核心。
3. 实现现场检查修复版。
4. 实现 FTP 静默检查修复回传版。
5. 整理通用 32 位记录清理版。
6. 验证现有报告整理和 Excel 汇总工具兼容性。
7. 完成 x86/x64 一致性、Win7 静态兼容和本机回归测试。
8. 整理干净的正式交付目录和使用说明。
9. 已完成Windows静默推送后的FTP扫描接收兼容诊断、真实FTP与SMB扫描区分及自动测试；无法自动判定端口范围时的处置策略已确认并实现，双端实机验收仍为上线前待办。

未经明确指示，不提交、不推送或上传 GitHub。
