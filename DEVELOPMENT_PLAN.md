# 终端安全检查与修复工具开发计划

状态：已确认并实施
计划版本：2.5
制定日期：2026-09-11（2026-09-19补充CEMS协同策略）
作者：倚栏听雨

## 1. 开发目标

减少现有程序的版本和使用方式混乱，将终端处理流程统一为：

```text
修复前检查 → 保存原始结果 → 修复系统异常 → 修复后复检 → 生成对比报告
```

新版本只开发两种检查修复入口：

1. 现场检查修复版。
2. FTP 静默检查修复回传版。

HTTPS 回传版本次不开发、不修改。浏览器密码、USB 历史和无线网络历史继续由独立的违规记录清理程序处理。

现场版和 FTP 版必须使用同一套检查核心和同一套修复核心。两种版本的检查项目、判断标准、修复范围、执行顺序、复检标准和报告结论必须保持一致，不能分别实现两套逻辑。两者只能在是否显示界面、是否需要人工确认、是否自动打开报告以及是否通过 FTP 回传方面存在差异。

## 2. 程序划分

### 2.1 现场检查修复版

建议文件名：`onsite-check-repair.exe`

运行流程：

1. 执行修复前检查。
2. 显示发现的异常和需复核项目。
3. 现场人员输入小写 `yes` 后执行修复。
4. 未输入 `yes` 时不修改系统，只保存修复前检查报告。
5. 修复完成后自动执行复检。
6. 生成并打开修复前后对比报告。
7. 不自动重启系统。

### 2.2 FTP 静默检查修复回传版

建议文件名：`ftp-check-repair.exe`

运行流程：

1. 网管系统以已提权的管理员或 SYSTEM 身份推送并运行单个 EXE。
2. 后台静默执行修复前检查。
3. 自动修复允许处理的系统安全异常。
4. 自动执行修复后复检。
5. 生成 HTML 和 JSON 对比报告。
6. 将报告上传到现有 FTP 服务器。
7. 上传失败时，在程序同目录保留报告和 `upload-status.json`。
8. 不显示窗口、不要求输入、不自动重启。

### 2.3 违规记录清理版

建议文件名：`record-cleaner.exe`

该程序继续独立保留，不合并到自动修复流程。它负责：

- 清理当前用户浏览器已保存密码。
- 清理 USB 存储设备历史。
- 清理无线网络和手机热点历史。
- 输入小写 `yes` 后才执行。
- 生成临时清理报告。

独立保留的原因：

- 浏览器密码与当前登录用户及用户加密上下文有关，以 SYSTEM 身份运行可能清理不到实际用户的数据。
- USB、无线和浏览器记录属于不可逆的数据删除，应当先保存检查证据再清理。
- 删除无线配置可能影响正在使用无线连接的终端。
- 系统安全配置异常不应自动触发用户数据删除。

## 3. 通用 32 位终端程序方案

远程无法提前判断终端是 32 位还是 64 位，因此正式终端交付统一采用 Win7 兼容的 32 位程序。

正式推送文件：

```text
onsite-check-repair.exe
ftp-check-repair.exe
record-cleaner.exe
```

支持范围：

- Windows 7 SP1 32 位和 64 位。
- Windows 10 32 位和 64 位。
- Windows 11 64 位。

在 64 位系统中，32 位程序通过 WOW64 运行。实现时必须保证：

- 需要时明确访问 64 位注册表视图。
- 服务管理使用原生 Win32 服务接口。
- 防火墙管理使用 Windows 防火墙 COM 接口。
- SYSTEM 注册表、NetBIOS、RDP 和网络配置不受文件系统重定向影响。
- 浏览器检查覆盖普通用户配置及 32 位、64 位浏览器安装路径。
- 32 位程序在 64 位系统上的检查和修复结果与 64 位参考构建一致。

64 位构建继续保留在开发测试环境中，只用于一致性验证，不再作为网管推送入口，避免同一台 64 位终端被重复运行。

## 4. 自动修复范围

现场版和 FTP 版只自动修复系统安全配置。

所有修复规则集中在共享修复核心中维护。现场版和 FTP 版不得各自保存独立的端口列表、服务列表、注册表路径或合规判断条件。以后增加或修改规则时，只修改共享核心，并由两个入口同时使用。

### 4.1 防火墙

- 启用域、专用、公用三个 Windows 防火墙配置文件。
- 保证下列入站阻断规则各保留一条：

```text
TCP：22、135、139、445、3389
UDP：137、138、3389
```

- 删除本程序自身产生的同名重复规则。
- 不停止 RPC/RpcSs 核心服务。
- 不阻断普通网页访问和客户端访问外部文件服务器的出站能力。

### 4.2 文件共享

- 停止 `LanmanServer` 服务。
- 将 `LanmanServer` 启动类型设置为禁用。
- 不删除原有共享定义，以便需要时回滚。

### 4.3 远程桌面

- 停止 `TermService` 服务。
- 将 `TermService` 启动类型设置为禁用。
- 将 `fDenyTSConnections` 设置为 `1`，禁止远程桌面连接。
- 同时保留 TCP、UDP 3389 入站阻断规则。

### 4.4 NetBIOS

- 枚举全部网卡配置。
- 将每个网卡的 `NetbiosOptions` 设置为 `2（禁用）`。
- 修复后逐项复检，明确区分“没有记录”和“读取失败”。

### 4.5 不自动处理的内容

- 不自动删除浏览器密码。
- 不自动删除 USB 历史。
- 不自动删除无线网络和手机热点历史。
- 不自动禁用无线网卡。
- 不自动重启系统。
- 不绕过域策略、安全软件或终端管理策略。

### 4.6 已完成修复：终端FTP扫描接收兼容性

现场反馈：Windows FTP静默检查修复版运行后，部分终端原有的“FTP扫描”功能失效。当前代码不会停止或禁用`FTPSVC`、IIS FTP或第三方FTP服务，也没有直接阻断TCP 21；但程序会启用域、专用、公用三个防火墙配置文件。若终端原来依靠关闭防火墙接收扫描文件，或者缺少FTP控制端口及被动数据端口的入站允许规则，启用防火墙后可能造成业务中断。另有部分设备界面虽称“FTP扫描”，实际使用SMB共享目录；此类功能会与停止`LanmanServer`、禁用NetBIOS及阻断137、138、139、445的现行安全要求直接冲突。

实现必须先完成诊断，再决定修复策略，不得直接放开TCP 21或大范围被动端口：

1. 在修复前报告中增加“扫描接收兼容性”检查，记录`FTPSVC`/IIS FTP、可识别第三方FTP服务、实际监听端口、当前防火墙配置文件状态及现有入站允许规则；不读取或输出FTP账号密码。
2. 区分真正的FTP接收与SMB共享扫描。FTP由服务、监听端口和防火墙规则证据确认；UNC路径、共享目录、`LanmanServer`或445端口依赖归为SMB，不得继续笼统标记为FTP问题。
3. 不停止或禁用已识别的FTP服务，不删除非本程序创建的FTP允许规则。程序自身的8条高危端口阻断规则继续保持不变。
4. 如果已存在范围明确且有效的FTP控制端口和被动端口允许规则，启用防火墙后保留这些规则并复检监听和入站可达性。
5. 如果检测到FTP服务，但无法确定控制端口、被动端口范围、允许来源或现有规则是否完整，不自动创建“任意来源+大端口范围”规则；报告为“需复核”。已确认策略：保留原本关闭的防火墙配置文件，不自动启用它们，不开放未知端口；其他安全修复继续执行。管理员后续提供受控端口和来源网段并完善允许规则后，再启用相应配置文件。
6. 如果实际为SMB扫描，明确报告其与当前“关闭Server服务和445端口”的安全基线冲突。默认不恢复SMB；需要继续使用的终端应进入经授权的业务例外清单，或另行确认受限来源、受限共享和审计方案。
7. 批量部署前，先在一台承担扫描接收业务的终端使用`ftp-check-repair.exe /audit-only`核对证据，再用默认模式完成扫描实机验收；实机验收通过后才能扩大推送范围。
8. 现场版和FTP静默版继续共用同一诊断与修复核心，不能只在FTP回传入口增加特殊规则。

实现状态（2026-09-16）：已完成共享兼容性核心、Windows服务/TCP监听/防火墙允许规则/SMB共享取证、报告明细和已确认的防火墙保护策略；已完成纯策略、报告合规、共享逻辑及Win7静态导入自动测试。真实扫描终端的控制连接、认证、数据连接和文件落盘仍需实机验收。

本项完成标准：既不削弱8条高危端口阻断要求，也不把未知端口范围自动放开；真实FTP接收终端在启用防火墙后仍能完成控制连接、认证、数据连接和文件落盘；SMB扫描冲突能够被准确识别并交由授权策略处理。

### 4.7 Windows与银河麒麟共用FTP接收端（已完成开发和自动测试，待三类实机验收）

目标是只保留一个运行在Windows服务器上的FTP接收服务，使用一个控制端口和一套被动端口范围，同时接收Windows终端与银河麒麟终端上传的HTML、JSON和上传状态文件，不再分别维护两个接收端和两个端口。

实施范围：

1. 以现有Windows FTP接收端为统一服务端基础，保留Windows客户端现有标准被动FTP上传行为。
2. 现有Windows接收端配置是唯一参数基准。服务器IP地址、监听地址、FTP控制端口、被动端口范围、FTP用户名、FTP密码、允许来源网段、报告保存目录和Windows防火墙规则均保持现状，不因接入银河麒麟报告而修改。
3. 银河麒麟客户端必须适配现有Windows参数和标准被动FTP行为；不得要求Windows接收端另开端口、另建账号、修改密码、扩大允许网段、改变报告目录或调整现有Windows客户端参数。
4. 如果银河麒麟现有增强长度或SHA-256命令与Windows接收端不兼容，优先修改银河麒麟客户端，使用现有Windows接收端已经支持的上传方式；不得为了保留麒麟专用协议而修改生产Windows参数。
5. FTP账号和密码继续沿用现有Windows接收端账号。密码只在授权的运行或部署环节提供，不写入报告、状态文件、开发计划、公开源码或公开配置样例。
6. Windows与银河麒麟报告保存在现有Windows报告目录，并统一使用现有Windows文件名格式`verification-report_<IP>_<MAC>_<时间戳>.html/.json`；平台类型写入JSON字段，不再依靠文件名前缀区分。
7. 银河麒麟上传状态文件使用不会被`verification-report_*.json`扫描误认为正式报告的独立前缀，避免Excel程序把状态文件统计为损坏报告。
8. 统一接收方案不得修改终端检查逻辑和修复逻辑；银河麒麟JSON只增加Windows报告管理所需的兼容字段，完整麒麟报告内容继续保留。
9. 历史`kylin-report_*`报告作为旧格式兼容输入保留识别能力，新生成报告不再使用该前缀。

开发顺序：

1. 记录现有Windows接收端参数和行为基线；测试使用脱敏副本，真实账号密码不写入测试代码或文档。
2. 固定现有Windows客户端标准`STOR`上传回归测试，确保后续修改前后行为完全一致。
3. 修改银河麒麟客户端，使其按现有Windows服务器地址、端口、账号认证、被动模式和文件落盘规则上传。
4. 只有在不修改任何现有Windows参数和Windows客户端行为的前提下，才允许对Windows接收端代码增加必要的文件格式兼容；能仅修改麒麟客户端解决的问题不得修改Windows接收端。
5. 更新银河麒麟客户端配置样例、启动脚本和README，参数含义与现有Windows接收端保持一致；敏感值不进入公开文件。
6. 适配并验证Windows报告去重、问题汇总及按人员汇总工具。
7. 先完成本机和虚拟环境测试，再分别使用一台Windows终端、一台银河麒麟x86_64终端和一台银河麒麟ARM64终端进行实机上传验收。

完成标准：不修改现有Windows接收端的IP地址、监听地址、控制端口、被动端口范围、用户名、密码、允许网段、报告目录、防火墙规则和Windows客户端参数；银河麒麟客户端使用这些既有参数成功上传；错误凭据、中断传输、超限文件和同名文件均得到明确且安全的处理；现有Windows上传不发生回归；三类实机报告均能在现有Windows报告目录中落盘并被后续工具正确识别。

### 4.8 银河麒麟CEMS防火墙协同（已完成代码和自动测试，待管理端配置及实机验收）

实机证据确认CEMS会在本程序退出后重建`INPUT`规则顺序；把`SEC_REMEDIATOR_INPUT`反复插入首位会与终端安全策略持续争抢。采用CEMS管理端补充方案：

1. 检测到`INPUT`由`CEMS_COMMON_INPUT`接管时，本程序只审计CEMS对8项目标端口的首个有效终止判决，不创建、移动或删除本地iptables链，也不修改、停用CEMS/KSC；仅清理本程序旧版本遗留的TCP/UDP 136规则。
2. 报告分别列出“CEMS已覆盖”和“CEMS管理端待补”项目；缺失项由管理员在CEMS策略中补充，程序不通过后置DROP制造“修复成功”的假象。
3. 当前实机规则快照中CEMS已覆盖6项，待补为`TCP 445`、`UDP 3389`；以每台终端当次规则审计结果为准，不把该清单硬编码成固定结论。
4. 未检测到CEMS接管时，保留原有`SEC_REMEDIATOR_INPUT`独立链修复路径，确保非CEMS麒麟环境行为不回退。
5. 已有本程序历史链不与CEMS争抢顺序；仅删除链内由本程序旧版本创建的TCP/UDP 136规则，其余规则继续不作为CEMS环境合规依据。

验收标准：CEMS环境连续运行三次除首次清理本程序遗留的136规则外，不产生其他本地iptables修改动作；报告稳定列出真实缺失项；管理端补齐后报告8项通过；CEMS/KSC规则数量、内容和顺序不因本程序改变；从另一台终端逐项探测确认8个目标端口均不可连接，并确认本程序不再创建TCP/UDP 136阻断规则。

## 5. 备份、幂等和回滚

- 第一次修复前保存防火墙、服务启动类型、服务运行状态、RDP 和 NetBIOS 原始状态。
- 已存在有效初始备份时不重复覆盖。
- 重复运行保持幂等，不重复创建防火墙规则。
- 修复后若配置被域策略或安全软件重新覆盖，应报告失败，不持续争抢策略。
- 保留 `/rollback`，只恢复本程序实际修改过的系统配置。
- 回滚不包含浏览器密码、USB 历史和无线历史等不可逆清理内容。

## 6. 报告格式和兼容性

继续使用现有报告文件名：

```text
verification-report_IP_MAC_时间.html
verification-report_IP_MAC_时间.json
```

报告内容分为三个阶段：

1. 修复前检查结果。
2. 修复动作及每项执行结果。
3. 修复后复检结果。

兼容要求：

- JSON 顶层 `summary` 和 `details` 保存修复后的最终结果。
- 新增修复前状态和修复过程字段，但不改变现有最终汇总字段的含义。
- HTML 明确对比每一项的修复前状态、修复动作和修复后状态。
- 报告不得包含 FTP 密码、浏览器密码、Cookie、令牌或其他敏感凭据。
- FTP 上传失败时保留完整本地报告和失败阶段、Win32 错误码。

## 7. 报告管理工具与跨平台统一计划

### 7.1 现有能力

- `ftp-report-pruner`：目前只识别Windows的`verification-report_*.html/.json`，按文件名中的终端标识和时间保留最新一组报告。
- `report-issue-exporter`：目前只读取Windows的`verification-report_*.json`，按IP选择最新报告，再依据人员对应表中的IP、MAC匹配持有人和组织机构，生成问题汇总Excel。
- 现有Windows报告去重、风险等级、人员匹配和Excel字段继续保持兼容，升级后不得改变已有Windows输入的结果。

### 7.2 Windows与银河麒麟报告统一管理（已完成开发和自动测试，待三类实机验收）

服务端固定运行在Windows系统。Windows客户端和银河麒麟客户端上传的报告全部保存到Windows接收端的同一个报告目录中，不按客户端操作系统拆分目录。

本项不开发任何银河麒麟报告管理软件，也不在银河麒麟客户端执行去重、汇总或生成Excel。新生成的Windows和银河麒麟报告统一使用现有Windows前缀`verification-report_*`。主要改造银河麒麟JSON输出，使其提供现有Windows管理程序所需的兼容字段；现有Windows程序`ftp-report-pruner-x64.exe`和`report-issue-exporter-x64.exe`只进行必要的小范围兼容修改。

目标是在同一个Windows混合报告目录中，由现有Windows管理程序完成统一的报告校验、按终端去重、人员匹配、问题提取、风险判断和Excel汇总。

统一处理流程：

```text
两类客户端上传到同一个Windows目录 → Windows去重程序识别两类报告并保留最新批次 → Windows汇总程序读取同一目录 → 匹配人员与组织机构 → 提取最终检查结果 → 生成统一Excel
```

#### 7.2.1 报告识别和字段适配

1. 新生成的两类报告统一采用`verification-report_<IP>_<MAC>_<时间戳>.html/.json`；管理程序继续使用现有Windows文件名解析规则，并通过JSON中的`platform`字段区分Windows和银河麒麟。
2. 银河麒麟JSON顶层增加与Windows管理接口一致的`ip`、`mac`、`computer`、`finished`、`summary`和`platform`字段；`summary`只提供简明检查项目、结论及风险映射所需信息，原有完整麒麟结构继续保留供HTML和人工核查使用。
3. Windows JSON增加或确认`platform=Windows`；银河麒麟使用`platform=Kylin`，平台字段缺失时只按旧Windows报告兼容处理，不根据IP、MAC或文件名猜测平台。
4. 修复报告默认使用修复后的最终检查结果；只读检查报告使用当前检查结果。修复前结果和修复动作保留在原报告中，不混入最终问题清单。
5. 统一结论为“通过、异常、需复核、不适用”；缺字段、格式错误或无法确认的内容不得当作“通过”。
6. 报告管理软件只读取报告，不修改原始HTML和JSON内容。
7. 为兼容已经生成的旧报告，Windows管理程序可以额外识别历史`kylin-report_*`，但新报告和正常处理主路径统一使用`verification-report_*`。

#### 7.2.2 跨平台去重规则

1. 将同一次运行生成的HTML、JSON和可选的`upload-status.json`视为一个报告批次，去重时整组保留或整组处理，禁止把不同时间的HTML和JSON拼成一组。
2. 终端身份优先使用“平台 + 规范化MAC”；MAC缺失时使用“平台 + IP + 计算机名”作为保守备用键，并在日志中标记身份依据不足。
3. 同一终端以报告文件名中的合法时间戳为主要排序依据，并用JSON中的检查时间交叉校验；时间冲突、文件损坏或只有单个文件的批次标记为需人工确认，不自动删除。
4. 同一个Windows去重程序对两类报告使用相同的预览、确认和删除流程；默认先预览，只有用户输入明确确认后才删除旧批次。
5. 去重日志分别记录保留、准备删除、跳过和无法识别的文件及原因，不能静默忽略坏报告。

#### 7.2.3 人员和组织机构匹配

1. 人员对应表文件名不固定，由用户运行时选择；表头至少包含“持有人、IP、MAC”，可选“组织机构、操作系统、计算机名”。
2. IP和MAC同时匹配时优先采用；否则允许唯一MAC匹配，再允许唯一IP匹配。任何一对多、多对一或冲突匹配均标记“未唯一匹配”，不得猜测人员。
3. MAC比较忽略大小写以及横线、冒号等常见分隔差异；IP必须按完整地址精确匹配。
4. 同一人员拥有多台终端时，终端明细表保留多行；人员汇总表再将该人员所有终端的问题合并，不能丢失具体IP、MAC和平台来源。
5. 未匹配人员的报告仍写入Excel，持有人和组织机构显示“未匹配”，便于后续补充基础表。

#### 7.2.4 问题和风险等级统一

1. Windows与银河麒麟的不同检查项名称映射到统一问题类别，不直接依赖中文显示文字进行风险判断。
2. 高危包括：高危端口未阻断、防火墙关键配置异常、文件共享服务未关闭、远程访问服务或策略未关闭、存在已保存无线网络连接信息。
3. 中危包括浏览器保存密码；无高危和中危项的其他明确异常列为低危。
4. “需复核”单独显示为“待核查”，不等同于低危、正常或检查通过；同一终端同时存在多种问题时，风险等级取最高级别。
5. Excel中的每条问题只保留能够说明问题性质的简明名称，不复制原报告中的预期结果、实际结果、证据详情、命令输出、修复动作或技术诊断过程。
6. 每条Excel记录保留源报告文件名，管理人员需要查看依据时打开对应HTML或JSON原报告；Excel本身不承担证据报告功能。

#### 7.2.5 Excel输出

由Windows版`report-issue-exporter-x64.exe`统一生成一个简明Excel工作簿。Excel只反映人员、终端、存在的问题和风险，不展开报告技术详情，至少包含：

- `终端问题汇总`：每台最新终端一行，只包含持有人、组织机构、平台、计算机名、IP、MAC、检查时间、异常问题、需复核问题、风险等级和源报告文件名。
- `人员问题汇总`：每个持有人一行，只汇总组织机构、终端标识、存在问题和最高风险等级；同一人员的多台终端问题合并但保留终端来源。
- `统计汇总`：只按平台、组织机构和风险等级统计终端数量、问题终端数量、未匹配人员数量及读取失败报告数量。

以下内容禁止写入Excel：检查证据、核查详情、预期结果、完整实际结果、命令输出、服务和端口枚举详情、USB日志原文、浏览器账号标识、修复动作以及其他原报告技术内容。去重过程、损坏文件、缺失配对文件和读取错误写入独立日志文件，不放入Excel工作簿。

#### 7.2.6 开发与测试顺序

1. 先修改银河麒麟报告生成器，使新报告采用`verification-report_*`并输出Windows管理接口兼容字段；Windows报告生成逻辑保持不变。
2. 使用脱敏的Windows、麒麟新格式和历史`kylin-report_*`JSON建立兼容测试，确认平台字段、终端身份、时间和简明问题能够正确读取。
3. Windows版`ftp-report-pruner-x64.exe`继续以现有文件名规则处理新报告，只补充上传状态文件排除和历史麒麟前缀兼容。
4. Windows版`report-issue-exporter-x64.exe`复用现有顶层字段和`summary`读取逻辑，只补充平台列、跨平台风险项目映射及历史麒麟格式兼容，并保持原Windows测试结果不变。
5. 增加人员多终端、组织机构、IP变化、MAC格式差异、重复IP、缺失MAC和匹配冲突测试。
6. 增加跨平台风险映射、异常与需复核并存、损坏JSON、缺少HTML、同时间戳和上传状态文件测试。
7. 验证生成的Excel能够由Microsoft Excel正常打开，中文、换行、筛选、列宽、风险颜色和统计公式显示正确，并确认工作簿中不存在原报告证据或技术详情。
8. 使用真实但已脱敏的Windows和银河麒麟报告副本完成验收；原始报告始终只读，测试输出写入独立临时目录。

完成标准：Windows和银河麒麟客户端报告上传到同一个Windows目录；现有两个Windows管理程序升级后能够正确识别并统一管理两类报告；每台终端只采用最新有效批次；人员、组织机构、平台、简明问题和风险等级能够准确写入同一个Excel；Excel不包含原报告证据和技术详情；同一人员的多台终端既保留逐台问题又能按人员汇总；任何损坏、冲突或无法匹配的数据均在独立日志中明确列出且不被误判为正常；交付目录中不存在麒麟版去重程序、麒麟版汇总程序或麒麟版Excel生成程序。

## 8. 交付目录规划

正式交付根目录固定为项目目录下的 `delivery`。所有新构建和更新先写入该目录；其他位置只允许在完成校验后人工复制，不作为构建源或优先交付目录。

```text
终端安全检查与修复工具
├─ 01-现场检查修复版
│  ├─ onsite-check-repair.exe
│  └─ README.txt
├─ 02-FTP检查修复回传版
│  ├─ ftp-check-repair.exe
│  └─ README.txt
├─ 03-违规记录清理版
│  ├─ record-cleaner.exe
│  ├─ record-cleaner-x64.exe
│  ├─ cleanup-remediator.ps1
│  ├─ wireless-remediator.ps1
│  └─ README.txt
└─ 04-报告管理工具
   ├─ ftp-report-pruner-x64.exe
   ├─ report-issue-exporter-x64.exe
   └─ README.txt
```

HTTPS 版本保留在源码和历史交付记录中，本次不修改，也不放入新的主要交付入口。

## 9. 版本管理建议

- 新终端套件当前发布版本为 `2.0.3`。
- 现场版和 FTP 版的产品版本为 `2.0.2.0`；记录清理版为 `2.0.1.0`，包含当前用户 IE Storage2/WinInet 保守清理和复读核验，并在 64 位 Windows 上自动使用 x64 辅助程序清理 USB 设备历史。
- 报告管理工具继续保留各自独立版本，并在交付清单中记录。
- 每次交付生成一份版本清单，记录文件名、文件版本、架构、SHA-256 和测试结论。
- 正式交付目录不包含源码、构建缓存、临时报告、测试日志、FTP 密码明文或签名私钥。
- 本地开发完成后不自动提交或上传 GitHub，必须等待明确指示。

## 10. 测试计划

### 10.1 公共行为接口

测试以下用户可观察行为：

1. 现场版能够完成修复前检查、确认、修复、复检和打开报告。
2. FTP 版能够静默完成检查、修复、复检、保存和上传报告。
3. 通用 32 位程序在 32 位和 64 位系统中得到一致的检查与修复结果。
4. 记录清理程序不会被检查修复流程自动调用。
5. 64 位 Windows 上的 USB 清理必须使用 `record-cleaner-x64.exe`；该文件缺失时明确报错，不回退到 WOW64 设备删除。

### 10.2 自动测试

- 检查无异常时不执行多余修改。
- 已存在规则时不重复创建。
- 重复防火墙规则能够收敛为一条。
- Server、远程桌面和 NetBIOS 异常能够被修复并通过复检。
- 部分修复失败时，报告准确标出失败项目。
- 无管理员权限时拒绝修复并返回明确退出码。
- FTP 上传成功和上传失败两种路径均保留准确状态。
- 新报告能够继续被重复报告整理工具和 Excel 汇总工具识别。
- x86/x64 参考构建的 `summary`、`details` 和修复结果一致。
- 在相同的模拟系统状态下，现场版和 FTP 版的修复前 `summary`、`details` 必须逐项一致。
- 在相同的模拟系统状态下，现场版和 FTP 版生成的修复动作清单、执行结果和失败原因必须逐项一致。
- 修复完成后，现场版和 FTP 版的最终 `summary`、`details` 和退出结论必须逐项一致。
- 一致性测试忽略界面提示、报告是否自动打开和 FTP 上传状态，只比较检查与修复业务结果；任何业务差异都必须使测试失败。
- Win7 导入表不得包含 `CreateFile2` 等不兼容入口点。
- FTP接收服务不存在时，新增兼容检查不得改变现有修复动作和报告结论。
- FTP服务存在且已有精确允许规则时，修复不得停止服务、删除允许规则或阻断已声明的控制端口和被动端口。
- FTP服务存在但端口或规则不完整时，必须标记需复核，且不得自动创建任意来源的大范围允许规则。
- SMB扫描场景必须识别为`LanmanServer`/共享目录依赖，不能误报为TCP 21问题。
- FTP扫描兼容逻辑在现场版和FTP静默版中的检查、动作与复检结果必须一致。
- Windows与银河麒麟客户端均按现有Windows接收端已经支持的标准被动FTP和`STOR`行为上传，不能为银河麒麟增加一套要求服务端改参数的专用生产路径。
- 统一接收方案必须覆盖错误凭据、中断传输、文件大小限制、不完整文件、同名隔离和现有多个允许来源网段。
- 报告去重、问题汇总和人员汇总工具必须同时识别`verification-report_*.json`与`kylin-report_*.json`。
- 开发前后逐项比较Windows接收端配置，确认IP地址、监听地址、控制端口、被动端口范围、用户名、密码、允许网段、报告目录、防火墙规则及Windows客户端参数均未改变；任何一项变化都视为测试失败。

### 10.3 真机验收

- Windows 7 SP1 x86。
- Windows 7 SP1 x64。
- Windows 10 x86。
- Windows 10 x64。
- Windows 11 x64。
- 普通管理员、已提权管理员和 SYSTEM 推送场景。
- 域策略覆盖、安全软件拦截、FTP 不通和重复运行场景。
- 真实FTP扫描接收：防火墙原为开启/关闭、主动/被动模式、控制端口21及自定义端口、限定被动端口范围、允许规则存在/缺失。
- 真实SMB扫描到共享目录：确认能够识别业务冲突，不以放开445或恢复`LanmanServer`冒充FTP兼容修复。

真机验收必须确认：

- 修复前报告准确。
- 修复动作与计划范围一致。
- 修复后报告准确。
- 从另一台电脑测试目标端口确实无法连接。
- 普通网页访问和客户端出站访问不受影响。
- 报告可以正常上传、整理并生成 Excel 汇总。
- Windows终端作为FTP接收端时，扫描设备能够完成连接、认证、数据传输和文件落盘；程序自身的FTP报告上传也保持正常。

### 10.4 Windows统一接收与报告管理专项测试和验收标准

本专项按“配置基线、上传协议、报告格式、去重管理、Excel汇总、实机回归”六个层级执行。所有强制项目全部通过后才能替换现有生产链路；任一项失败均不得上线。

#### 10.4.1 Windows配置基线不变

开发前记录现有Windows接收端以下基线：服务器IP和监听地址、控制端口、被动端口范围、用户名、允许网段、报告目录、防火墙规则、启动方式及Windows客户端参数。密码只由授权人员确认仍为原密码，不在测试记录中显示、复制或计算可外传的摘要。

验收标准：

- 开发前后逐项一致，没有新增端口、账号、目录、允许网段或防火墙放行。
- 现有Windows客户端不修改配置即可继续上传。
- 接收端停止、启动和异常退出后的行为与现版本一致。
- 任一Windows参数被修改，专项验收立即判定失败。

#### 10.4.2 FTP上传自动测试

必须覆盖：

- 现有Windows客户端使用原参数上传HTML和JSON成功。
- 银河麒麟x86_64和ARM64客户端使用同一组现有Windows参数、标准被动FTP和`STOR`上传成功。
- 正确账号密码可以登录；错误用户名或密码必须被拒绝，且不得留下可被当成正式报告的文件。
- 允许网段内正常上传；现有允许网段外的来源继续被拒绝，不扩大访问范围。
- 传输中断、超限文件、零字节文件和不完整文件不能覆盖已有有效报告，临时文件能够安全清理或明确记录。
- 同名文件不会静默覆盖另一份有效报告；处理结果必须明确且可追踪。
- HTML和JSON均落到现有Windows报告目录，不创建麒麟专用目录。

通过标准：每个用例的服务端文件、客户端退出码和上传状态一致；失败场景不产生伪成功报告，Windows原有成功路径结果与开发前基线一致。

#### 10.4.3 报告文件和JSON兼容测试

- 两类新报告均使用`verification-report_<IP>_<MAC>_<时间戳>.html/.json`。
- JSON包含有效的`platform`、`ip`、`mac`、`computer`、`finished`和`summary`字段；Windows为`platform=Windows`，银河麒麟为`platform=Kylin`。
- HTML与JSON的IP、MAC、时间、检查结论相互对应，不允许跨批次错配。
- 上传状态文件不匹配正式报告扫描规则，不会被当成损坏报告。
- 历史`kylin-report_*`能够作为旧格式读取，但不会影响新格式的正常处理。

#### 10.4.4 Windows去重程序验收

在同一个测试目录放入Windows、麒麟新格式、麒麟旧格式、重复批次、损坏JSON、缺少HTML或JSON及上传状态文件。

通过标准：

- 能正确识别两种平台和每个运行批次。
- 同一终端只保留时间最新的完整批次，HTML和JSON成组处理。
- 损坏、身份不完整、时间冲突或缺少配对文件的批次不自动删除，并在日志中写明原因。
- 默认预览不删除任何文件；明确确认后只删除预览清单中的旧批次。
- 原始有效报告内容保持不变，去重结果可重复执行且第二次运行无额外删除。

#### 10.4.5 Excel汇总程序验收

使用包含持有人、组织机构、IP和MAC的人员对应表，覆盖同一人员多终端、MAC格式差异、IP变化、重复IP、缺失MAC、匹配冲突和未匹配人员。

通过标准：

- 每台终端只采用最新有效报告，并准确匹配持有人和组织机构。
- IP和MAC冲突或无法唯一匹配时显示“未匹配”或“待核查”，不得猜测人员。
- Windows和银河麒麟问题使用同一风险规则，最高风险等级计算正确。
- Excel仅包含人员、组织机构、终端标识、平台、简明问题、需复核问题和风险等级。
- Excel不包含检查证据、完整实际结果、命令输出、端口或服务枚举、USB日志、浏览器账号标识和修复动作。
- 同一人员多台终端既能逐台查看问题，也能看到人员级最高风险汇总。
- Microsoft Excel能够正常打开，中文、筛选、换行、列宽和风险颜色显示正确。
- 读取失败、坏报告及去重说明只进入独立日志，不污染Excel内容。

#### 10.4.6 三类实机最终验收

至少使用一台当前正常回传的Windows终端、一台银河麒麟V10 SP1 x86_64终端和一台银河麒麟V10 SP1 ARM64终端。三台设备均使用现有Windows接收端的原IP、原端口、原账号密码和原允许网段。

通过标准：

- 三台终端分别完成检查、生成HTML和JSON并上传到同一个Windows目录。
- Windows终端上传行为、耗时、文件命名和报告内容没有回归。
- 两台银河麒麟终端不安装额外运行依赖即可上传。
- Windows去重程序正确保留三台终端的最新完整报告。
- Windows Excel程序正确对应人员、组织机构、简明问题和风险等级。
- 连续重复运行三次不会产生错误覆盖、错误去重或重复人员统计。
- 验收后再次核对Windows配置基线，所有冻结参数仍保持不变。

上线门槛：自动测试、混合目录测试和三类实机验收全部通过，并保留现有Windows接收端程序和配置备份以便立即回退。构建成功、单平台成功或仅能生成报告均不能替代完整验收。

## 11. 完成标准

- 正式终端只需要识别三个用途明确的通用 EXE。
- 网管远程只推送一次 FTP 通用版，不需要判断系统位数。
- 现场版和 FTP 版使用同一个检查核心和同一个修复核心。
- 两种版本的检查项目、合规判断、修复动作、失败处理和复检结论保持一致；版本差异仅限交互和回传方式。
- 修复前、修复过程和修复后结果可以在一份报告中准确对比。
- 现有重复报告整理和 Excel 汇总工具继续兼容。
- 所有自动测试通过。
- Win7 静态兼容检查通过。
- 目标系统真机测试结果单独记录，不用构建成功代替真机验收。

## 12. 开发边界

本计划确认前不修改现有程序。收到“确认可以开始”后，按以下顺序实施：

1. 固定检查、修复和报告的公共测试接口。
2. 提取共享系统修复核心。
3. 实现现场检查修复版。
4. 实现 FTP 静默检查修复回传版。
5. 整理通用 32 位记录清理版。
6. 验证现有报告整理和 Excel 汇总工具兼容性。
7. 完成 x86/x64 一致性、Win7 静态兼容和本机回归测试。
8. 整理干净的正式交付目录和使用说明。
9. 已完成Windows静默推送后的FTP扫描接收兼容诊断、真实FTP与SMB扫描区分及自动测试；无法自动判定端口范围时的处置策略已确认并实现，双端实机验收仍为上线前待办。
10. 已完成Windows与银河麒麟共用FTP接收端所需的客户端兼容改造和自动测试；现有Windows接收端全部参数冻结并作为唯一标准，只允许银河麒麟客户端适配。三类实机上传验收通过前，现有两个接收端继续独立保留，不提前替换生产链路。
11. 已完成现有Windows报告管理工具的跨平台兼容改造，使同一Windows目录中的Windows与银河麒麟报告能够统一识别、成组去重、匹配人员和组织机构并生成统一Excel；未开发麒麟版管理软件。混合目录自动回归测试已通过，真实脱敏报告和三类实机验收仍为上线前待办，现有Windows报告管理流程继续作为可回退版本保留。
12. Windows 浏览器审计补充 Internet Explorer 的 Web Credentials、Storage2 和 WinInet 凭据索引；当前用户存在记录时标记异常，SYSTEM 无法读取登录用户凭据时标记需复核。仅输出网站和账号元数据，不读取或解密密码；现场版与 FTP 版继续复用同一审计核心。

未经明确指示，不提交、不推送或上传 GitHub。
