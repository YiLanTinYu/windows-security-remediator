# 高危端口阻断工具项目交接文档

## 1. 项目基本信息

- 项目名称：高危端口阻断工具（Security Remediator）
- 当前版本：1.1.0.0
- 作者：倚栏听雨
- 项目目录：`C:\运维小工具\高危端口阻断`
- 交付目录：`C:\运维小工具\高危端口阻断\dist`
- 目标系统：Windows 7 SP1、Windows 10、Windows 11
- 程序架构：32 位原生 Win32，可运行在 32 位和 64 位 Windows
- 运行依赖：无须安装 .NET、PowerShell 或 VC++ 运行库

## 2. 项目用途

程序用于通过网管平台静默加固 Windows 终端，关闭高危入站端口、文件共享、NetBIOS 和远程桌面服务。

支持网管代理使用以下权限执行：

- LocalSystem / SYSTEM
- 已提权的 Administrators 组成员

普通用户运行时不会弹出 UAC，程序返回权限不足。

## 3. 当前阻断范围

### TCP 入站

- 22
- 135
- 139
- 445
- 3389

### UDP 入站

- 137
- 138
- 3389

### 系统加固

- 启用 Windows 防火墙的域、专用和公用配置文件。
- 停止并禁用 `LanmanServer`（Server）服务。
- 停止并禁用 `TermService`（远程桌面）服务。
- 设置 `fDenyTSConnections=1`，拒绝远程桌面连接。
- 在所有网卡上设置 `NetbiosOptions=2`，禁用 NetBIOS over TCP/IP。
- 保留 RPC 核心服务，不停止 `RpcSs`。
- 只阻断入站，不主动阻断客户端出站访问。

## 4. 交付文件

`dist` 目录中的主要文件：

```text
remediator.exe             主程序
verify-remediator.bat      验证入口
verify-remediator.ps1      验证逻辑
README.txt                 用户说明
app-icon.png               图标预览
remediator.ico             Windows 图标
```

`remediator.exe` 已嵌入程序图标和版本资源：

- 文件版本：1.1.0.0
- 产品版本：1.1.0.0
- 公司/作者：倚栏听雨
- 文件说明：Windows 高危端口静默修复程序

## 5. 运行命令

### 检测并修复

```bat
remediator.exe
remediator.exe /apply
```

不带参数时默认执行 `/apply`。

### 只检测

```bat
remediator.exe /audit
```

### 回滚

```bat
remediator.exe /rollback
```

### 指定日志目录

```bat
remediator.exe /apply /log-dir "D:\SecurityLogs"
```

## 6. 日志和返回结果

默认写入 `remediator.exe` 所在目录：

```text
remediator.log       程序运行日志，每次运行覆盖
verification.log     验证日志
last-result.json     最近一次机器可读结果
result.txt           最近一次中文摘要
```

日志首行包含程序版本和作者：

```text
SecurityRemediator version 1.1.0, author: YiLanTingYu, started
```

### 退出码

| 退出码 | 含义 |
|---:|---|
| 0 | 执行成功 |
| 1 | 修复或回滚至少一项失败 |
| 2 | 参数错误 |
| 3 | 权限不足 |
| 4 | 修复完成但需要稍后重启 |
| 5 | 审计发现不合规项 |

成功示例：

```json
{"exitCode":0,"mode":"apply","changed":true,"failures":0}
```

## 7. 网管推送建议

建议配置：

- 执行账户：SYSTEM 或已提权管理员
- 执行命令：`remediator.exe /apply`
- 显示方式：隐藏窗口、等待程序退出
- 成功条件：退出码为 0 或 4
- 结果采集：读取同目录的 `last-result.json`
- 文件落盘后再启动，不依赖 SMB、WMI、RDP 或 SSH 保持连接

注意：程序会阻断 TCP 22 和 TCP 3389。推送前必须确认网管代理不依赖 SSH 或远程桌面维持任务。

## 8. 验证方法

运行：

```bat
verify-remediator.bat
```

验证脚本会检查：

- Server 服务状态
- 远程桌面服务状态
- RDP 注册表配置
- SecurityRemediator 防火墙规则
- NetBIOS 配置
- 本机相关端口监听状态

本机存在监听不等于防火墙阻断失败。最终应从另一台电脑测试 TCP 22、135、139、445、3389，以及使用网管扫描器验证 UDP 137、138、3389。

## 9. 回滚说明

修复前，程序将服务、RDP 和 NetBIOS 等原状态保存到：

```text
%ProgramData%\SecurityRemediator\backup.state
```

执行 `/rollback` 时：

- 删除本程序创建的防火墙规则。
- 恢复 Server 和远程桌面服务原启动类型及运行状态。
- 恢复 RDP 注册表原值。
- 恢复各网卡 NetBIOS 原值。

程序不会自动重启计算机。

## 10. 构建方式

开发环境：

- Visual Studio 2026 Community / MSVC
- Windows SDK 10.0.26100.0
- CMake

由于中文路径可能导致链接器 PDB 路径异常，建议使用纯英文构建目录：

```bat
cmake -S "C:\运维小工具\高危端口阻断" ^
  -B "C:\Build\SecurityRemediator" ^
  -G "NMake Makefiles" ^
  -DCMAKE_BUILD_TYPE=Release

cmake --build "C:\Build\SecurityRemediator"
```

构建目标使用 `/MT` 静态运行库和 Win32/x86 架构。

## 11. 当前限制和风险

- 当前端口阻断通过 Windows 防火墙规则实现，尚未改为 IP 安全策略或组策略。
- `/rules` 和 `/signature` 外置签名规则接口尚未正式启用。
- `rule-signer` 仍为开发占位工具，不应部署到终端。
- 程序会禁用文件共享、打印共享和远程桌面。
- 阻断 TCP 22 可能影响 SSH 运维。
- 域策略和第三方安全软件可能覆盖本地防火墙设置。
- Windows 7、Windows 10、Windows 11 的正式批量部署前仍应分别进行虚拟机和小范围灰度验证。

## 12. 交接检查清单

- [ ] 确认网管代理使用 SYSTEM 或已提权管理员权限。
- [ ] 确认网管代理不依赖 TCP 22、135、139、445 或 3389。
- [ ] 在 Win7 SP1、Win10、Win11 测试机分别执行 `/audit` 和 `/apply`。
- [ ] 从另一台电脑验证目标端口不可访问。
- [ ] 执行一次 `/rollback` 并确认原配置可恢复。
- [ ] 保存正式发布的 EXE 哈希值和版本号。
- [ ] 灰度验证完成后再扩大推送范围。
